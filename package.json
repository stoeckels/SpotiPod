{
  "name": "spotipod",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run frontend:dev",
    "frontend:install": "npm --prefix src/frontend install",
    "frontend:dev": "npm --prefix src/frontend run dev",
    "frontend:build": "npm --prefix src/frontend run build",
    "frontend:start": "npm --prefix src/frontend run start"
  }
}
